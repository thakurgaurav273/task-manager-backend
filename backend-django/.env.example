# Django Environment Variables
# Copy this file to .env and update with your actual values

# Database Configuration
# Django needs to connect to the same database as Laravel
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=taskmanager
DB_USERNAME=root
DB_PASSWORD=

# If your MySQL root user has a password, set it above
# Common passwords: root, password, or leave empty if no password

# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-change-this-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
